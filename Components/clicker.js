import React from "react";
import {Text, View} from "react-native";

export default class Clicker extends React.Component{
    render(){
        return(
            <View>
               <Text style={{color:"#FFF"}}>Lol</Text>
            </View>
        )
    }
}